1) install an static http server global
    $ npm install -g http-server
    
2) Install Gulp locally in dev
    $ npm install gulp --save-dev -e

3) Install gulp globally 
    $ npm install gulp -g
    
4) Create the gulp file
    gulpfile.js

5) to run a gulp task (make sure gulpfile.js exist)
    $ gulp taskName

6) run all task 
    $ gulp

7) install gulp concat plug in (use for concatenate files)
    $ npm install gulp-concat --save-dev
